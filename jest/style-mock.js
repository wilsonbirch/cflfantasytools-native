// Stub for CSS imports under Jest. Expo's Metro bundler understands CSS, but
// Jest's transform does not — components only import these for their side effect
// (web styling), so an empty module is the correct test-time stand-in.
module.exports = {}
