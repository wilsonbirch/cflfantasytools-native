# Where the api lives. Simulators and devices cannot reach "localhost", so on a
# real device this must be your machine's LAN address or a deployed api.
EXPO_PUBLIC_API_URL="http://localhost:4000/graphql"
