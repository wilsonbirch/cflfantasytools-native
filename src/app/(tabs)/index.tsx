import { Text, View } from 'react-native'
import { ui } from '~/lib/ui'

// Placeholder; the Teams screen lands in the next stacked PR.
export default function Teams() {
    return (
        <View style={ui.center}>
            <Text style={ui.muted}>Teams coming soon.</Text>
        </View>
    )
}
