import { Text, View } from 'react-native'
import { ui } from '~/lib/ui'

// Placeholder; the Stats screen lands in the next stacked PR.
export default function Stats() {
    return (
        <View style={ui.center}>
            <Text style={ui.muted}>Stats coming soon.</Text>
        </View>
    )
}
